#!/bin/sh

HOME="/home/$USER"
FILE="$HOME/.cache/doppler.gif"

testconn () {
	ping -q -c 1 example.org 2>/dev/null 1>&2
}

if testconn; then
	curl --silent "https://radar.weather.gov/ridge/lite/KBOX_loop.gif" > "$FILE"
fi
